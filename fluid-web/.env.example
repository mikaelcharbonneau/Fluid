# Copy to .env.local for local development.
# Phase 1 (clickable prototype) needs no environment variables.
#
# Reserved for upcoming phases:
# Phase 2 — auth + persistence:
#   NEXT_PUBLIC_SUPABASE_URL=
#   NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Phase 3 — real generation:
#   ANTHROPIC_API_KEY=
